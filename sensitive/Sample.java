package com.example;

/**
 * Sample class to test keyword/param scanning.
 */
public class Sample {

    // ─────────────────────────────────────────────────────────────
    // 1)  keyword YES, param YES   →  "keyword yes param yes"
    public void encryptFunction(String keyPrivate) {
        // the keyword "dog" appears here
        int count = 0;
        if (amount != null) {
            count++;
        }
    }
    }
}
